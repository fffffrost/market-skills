# MARKET//SKILLS

## 项目目标

为中文市场从业者提供经过实战提炼、可直接安装的 AI Agent Skills。网站按真实任务组织内容，帮助用户理解什么时候使用、需要提供什么、最终会得到什么。

## 当前状态

- 状态：首页系统概览字号与雷达动画、10 个详情页中文执行流程及全站 SEO/GEO 技术优化已发布到 GitHub，并通过固定 CLI 部署流程同步到腾讯云受限环境；GitHub CI、版本约定和上线运行手册已建立；ICP备案的腾讯云审核已通过，域名实名认证已通过但未满 3 天，等待腾讯云再次提交管局；备案前受限公网验收已通过；Skill 雇主与产品信息隔离审计已完成
- GitHub：https://github.com/fffffrost/market-skills
- 域名：`mktskill.com`
- 云资源：腾讯云上海轻量应用服务器，2 核 2G、3M、40GB、包年
- ICP 备案：腾讯云审核已通过；因域名实名认证未满 3 天暂未提交管局，系统预计在实名后第 3 天 10 点前再次尝试；提交管局后仍需完成工信部短信核验与管局审核
- 产品名：MARKET//SKILLS
- 副标题：市场人的 AI Skill Hub
- 技术方案：Next.js App Router、TypeScript、静态导出、Nginx、客户端搜索筛选
- 迭代路线：见 `ROADMAP.md`；依次推进上线闭环、可信度与反馈、工作路径与需求驱动扩展、有限贡献试点
- 授权：MIT
- 页面：首页、静态 Skill 目录、10 个静态详情页、安装指南、404、sitemap、robots
- 内容：10 个第一方 Skill，覆盖洞察、策略、内容、执行和复盘

## 已完成能力

- 中文/英文关键词搜索，以及阶段、岗位、任务组合筛选。
- 单个与整包安装命令、复制失败回退、手动安装路径和源码审查提示。
- 每个 Skill 均包含 `SKILL.md`、`listing.yaml`、`agents/openai.yaml` 和任务模板。
- 构建期校验名称、元数据、引用、版本、授权和正负向验收场景。
- 深色 AI 操作台视觉，支持键盘焦点、减少动效偏好与 375px 移动端。
- 全站正文采用面向中文连续阅读的 16–18px 字级、1.65–1.8 行高与更高对比度；标签和状态码保持紧凑层级。
- 首页系统概览面板的状态、参数与保障项采用 10–13px 字级和更高对比度，兼顾操作台风格与可辨识度。
- 首页系统概览雷达使用圆环与核心数字启动、周期扫描光带和错峰信号点动画，并适配系统减少动效偏好。
- 10 个 Skill 详情页使用独立中文执行流程元数据展示真实工作步骤，不再直接暴露 `SKILL.md` 的英文结构标题。
- 13 个可索引页面使用独立 T/D、自引用 canonical、页面级 OG/Twitter 元数据和统一 1200×630 分享图；筛选参数统一规范到技能库主 URL。
- 首页、技能库、安装指南和 Skill 详情页分别提供 WebSite、CollectionPage、HowTo、BreadcrumbList 与 SoftwareSourceCode JSON-LD。
- sitemap、canonical 与站内静态路径统一使用尾斜杠，并由构建后 SEO 校验阻止元数据缺失、重复或 URL 不一致的版本发布。
- GitHub CI 在每次 `main` 推送和 Pull Request 中使用干净环境执行完整 `npm run verify`。
- 腾讯云发布固定使用 `npm run deploy:tencent`：生产构建后通过 SSH 与 `rsync` 上传独立 release，校验文件数与首页哈希，再切换 Nginx `current` 软链接并执行远端冒烟测试。
- 10 个 Skill 的当前文件已排除雇主、产品、客户、内部指标、公司域名和可追溯独特表述。
- 生产环境缺少真实 GitHub 仓库配置时主动阻止发布。

## 重要决定

- 中文职场场景优先，英文 slug 与安装命令保持跨平台兼容。
- 按洞察、策略、内容、执行、复盘五个工作阶段组织，而不是按工具或模型分类。
- 首版没有账号、投稿、收藏、评论、排行榜、付费、CMS 或服务端数据库。
- 外部作者的配图和封面 Skill 未纳入第一方首发。
- 公开 GitHub 仓库固定为 `fffffrost/market-skills`；生产站点部署在腾讯云上海轻量应用服务器，由 Nginx 托管静态导出文件。
- 生产公开配置写入 `.env.production`；域名解析与 HTTPS 在 ICP 备案通过后启用。
- 备案审核期间，HTTP 80 仅向当前管理端公网 IP 开放；备案通过并完成安全检查后再正式对公网开放。
- 服务器 SSH 已禁用密码和键盘交互认证，root 仅允许公钥登录；其他设备需要单独配置公钥授权。
- 可读性调整优先覆盖连续正文、任务说明和操作步骤；版本号、标签、状态码等辅助元数据不随正文等比例放大。
- 公开 Skill 必须与当前雇主和产品资料隔离；通用方法可以保留，但不得包含名称、内部案例、指标、联系信息或可反查来源的独特措辞。
- Agent 使用的 `SKILL.md` 指令结构与中文站点展示解耦；详情页流程由 `listing.yaml` 的中文元数据维护，并在构建期校验。
- GEO 继续建立在可抓取静态 HTML、标准 SEO、真实页面内容和结构化数据上，不添加缺乏通用标准支持的专用 AI 标记；当前 robots 继续允许所有爬虫，GPTBot 训练权限留待正式开放前单独确认。
- ICP 只阻塞域名正式公网开放，不阻塞 GitHub 发布、内容完善、CI 和上线预案；后续版本按验证门槛推进，不以等待备案为理由停工。
- 目录扩展前先解除校验器对 10 个 Skill 和固定 `Version 1.0.0` 的写死约束；新 Skill 由真实需求信号驱动，不按数量扩张。
- 腾讯云 CLI 与 TAT 用于云资源管理或远程命令；站点构建产物固定通过 SSH 与 `rsync` 发布。Nginx 只指向 `/var/www/market-skills-current`，旧 release 默认保留以支持显式回滚。
- 网站发布使用 `site-vX.Y.Z` Git 标签，单个 Skill 继续独立使用语义化版本；正式 `site-v1.0.0` 标签在备案通过和域名验收完成后创建。

## 验证状态

- `npm run verify`：通过。
- Skill：10/10 通过项目校验与官方 `quick_validate.py`。
- 组件测试：4/4 通过。
- 浏览器测试：桌面与 375px 移动端共 12/12 通过，包含主要正文、首页系统概览字号与动画、详情页中文执行流程、canonical、社交元数据与 JSON-LD 回归检查。
- 生产构建：20 个静态页面/资源导出到 `out/`，当前本地导出包含 177 个站点文件，目录页、结构化数据与构建期分享图均无服务端运行依赖。
- SEO：13 个可索引页面的 T/D 均唯一，canonical、OG/Twitter 图片和 JSON-LD 完整且可解析；sitemap 共 13 条规范 URL，构建后专项校验通过。
- 腾讯云：Ubuntu 24.04.4 LTS、Nginx 1.24.0、`rsync` 与 TAT 运行正常；提交 `28519a1` 已部署为 release `20260722131625-28519a1`，共 177 个站点文件，无 `.DS_Store` 或 AppleDouble 文件；Nginx 通过 `current` 软链接提供服务并保留旧 release。
- 公网验证：受限来源下首页、目录、详情页、安装指南、robots 和 sitemap 均为 200，不存在页面为 404；首页包含新版内容且公网与本地构建 SHA-256 一致；分享图返回 200 与 `image/png`，静态资源缓存规则正常。
- SSH：全新密钥会话登录成功，密码专用登录被拒绝；服务端有效配置为公钥认证开启、密码与键盘交互认证关闭。
- 安装：已在隔离的临时 Git 项目中验证本地源和公开 GitHub 源；单个 Skill 安装为 1/1，整包安装为 10/10。
- 视觉：已检查首页桌面、首页移动端和 Skill 详情长页截图；可读性迭代后复查了桌面技能卡、375px 详情页关键正文区、系统概览面板及雷达启动与持续扫描状态、中文执行流程，以及 1200×630 社交分享图的实际 PNG 渲染。
- GitHub：公开仓库 `main` 已包含当前 Launch Candidate、CI、CLI 部署脚本和运行手册；当前腾讯云 release 对应提交 `28519a1`，首发代码提交为 `797058698dde`。
- 内容隔离：当前 `skills/` 的 10 个 Skill、40 个受版本控制文件已完成名称、联系信息、路径、内部指标和工作资料连续措辞检查；当前工作树零命中。初始公开提交中的 1 处通用表述已由用户确认不构成敏感信息，且当前文件已改为更通用的说法，无需重写 Git 历史。

## 明确下一步

1. 增加“安装失败”“Skill 问题”“建议新 Skill”三类 GitHub Issue 模板与站内反馈入口。
2. 准备 3 个代表性 Skill 的合成案例节选和适用边界说明。
3. 关注腾讯云再次提交管局的状态；提交后在要求时限内完成工信部短信核验。
4. 正式开放前确认 GPTBot 训练抓取策略；OAI-SearchBot 保持允许以支持 ChatGPT 搜索发现。
5. 备案通过后解析 `mktskill.com`、开放 Web 生产端口并签发 HTTPS 证书，完成监控、安装和收录验收，再创建 `site-v1.0.0` 标签。
