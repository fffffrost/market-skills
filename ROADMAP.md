# MARKET//SKILLS 迭代路线图

更新时间：2026-07-22

## 路线原则

- ICP 只阻塞域名正式公网开放，不阻塞 GitHub 发布、内容完善、测试、CI 和上线预案。
- 版本按可验证结果推进，不为赶时间同时扩功能、扩内容和改基础设施。
- 网站版本与单个 Skill 版本分开管理：网站发布使用 `site-vX.Y.Z` 标签；每个 Skill 继续在自身元数据中使用独立语义化版本。
- 首轮公开数据只建立基线，不先写脱离真实流量的增长目标。
- 在现有静态架构能够解决问题时，不引入账号、数据库或内容管理后台。

## 核心观察指标

1. **发现**：被收录页面数、自然搜索进入页、非品牌搜索词。
2. **安装意向**：安装指南到达、安装命令复制、GitHub 源码访问。复制和访问只代表意向，不等同于安装成功。
3. **使用质量**：安装失败反馈、Skill 问题反馈、重复出现的边界误解、发布后回归故障。
4. **需求信号**：无结果搜索词、Skill 建议、真实任务反馈，以及同一需求是否由两个以上独立信号支持。

正式开放后的前 14 天用于建立基线，再决定具体目标值和新 Skill 优先级。

## site-v1.0.0 — 上线闭环

**目标**：让当前 10 个 Skill 在正式域名下稳定、可安装、可索引、可回滚。

备案等待期完成：

- 发布当前已验证的本地版本到 GitHub 和腾讯云受限环境，避免大量未提交改动长期悬空。
- 增加 GitHub CI，在干净环境执行 `npm ci` 与 `npm run verify`。
- 建立网站发布标签与简短变更记录，明确网站版本和 Skill 版本的边界。
- 补齐上线运行手册：DNS、80 到 443 跳转、HTTPS 证书及续期、防火墙、回滚、日志轮转和外部可用性检查。
- 增加轻量反馈入口与 GitHub Issue 模板，至少覆盖“安装失败”“Skill 问题”“建议新 Skill”。
- 确认 GPTBot 训练抓取策略；OAI-SearchBot 保持允许。

备案通过后完成：

- 解析 `mktskill.com`，开放正式 Web 端口，签发 HTTPS 证书并验证自动续期。
- 从全新临时项目复验单个 Skill 与整包安装。
- 验证首页、技能库、10 个详情页、安装指南、404、robots、sitemap、canonical 和分享图。
- 启用最小化的可用性监控与日志留存；未明确数据需求前不接入重型第三方分析。
- 向 Google、百度和 Bing 站长平台提交站点与 sitemap。

完成门槛：

- GitHub CI 在干净检出上通过；`main` 有可追溯的 `site-v1.0.0` 发布点。
- `https://mktskill.com/` 可访问，HTTP 正确跳转到 HTTPS，证书、静态缓存和自定义 404 正常。
- 13 个规范页面可抓取且元数据校验通过；单个与整包安装再次成功。
- 监控能够发现站点不可用，运行手册能够完成发布与回滚。

## site-v1.1.0 — 可信度与反馈

**目标**：回答用户“这个 Skill 是否适合我、会产出什么、遇到问题去哪里反馈”。

- 先为 `research-competitors`、`wechat-article-editor`、`campaign-retrospective` 制作脱敏或合成的输入与输出节选，覆盖洞察、内容和复盘三类任务。
- 在详情页展示适用边界、最低输入要求和常见失败原因，不把 Skill 描述成万能自动化。
- 补充安装故障排查和兼容宿主说明；所有命令继续以公开仓库的实际安装验证为准。
- 记录安装命令复制、GitHub 跳转和无结果搜索等最少必要事件；若采集任何访客数据，先明确用途、留存和隐私说明。
- 在上线后第 14 天做第一次数据与反馈回顾，形成下一轮内容优先级。

完成门槛：

- 3 个代表性 Skill 均有可审查的案例节选与边界说明。
- 用户能在两次点击内提交三类反馈；安装故障有明确排查路径。
- 完成首份基线复盘，能够区分“有访问”“有安装意向”和“已验证安装成功”。

## site-v1.2.0 — 工作路径与需求驱动扩展

**目标**：从单个 Skill 目录升级为可连续执行的市场工作路径，并只扩充被需求证明的内容。

- 用现有 Skill 先组织 3 条静态工作路径：产品发布、内容生产与分发、活动策划与复盘。
- 每条路径明确输入清单、Skill 顺序、人工决策点、交接物和最终产出；不增加服务端状态。
- 在增加第 11 个 Skill 前，解除校验器对“恰好 10 个目录”和固定 `Version 1.0.0` 文案的写死约束，改为按元数据验证目录与版本一致性。
- 新 Skill 只有在至少两个独立需求信号支持时进入开发；候选假设包括内容选题与排期、客户案例写作、落地页诊断，最终以首轮数据为准。
- 新 Skill 必须同时提供任务模板、正负向验收场景、中文站点元数据和合成案例，并通过项目校验与官方校验。

完成门槛：

- 3 条工作路径可从头执行，且没有引入账号、数据库或隐藏状态。
- 目录数量和 Skill 版本不再写死，新增内容能被 CI 自动发现并验证。
- 每个新增 Skill 都能追溯到需求证据，并通过完整验证与内容隔离检查。

## site-v1.3.0 — 有限贡献试点

**目标**：验证外部贡献能否在不牺牲质量和维护成本的前提下成立。

- 发布贡献指南、Skill 模板、质量清单、来源与授权声明要求。
- 用 CI 检查目录结构、元数据、引用、测试、版本和站点构建。
- 区分第一方与外部贡献内容，并保留人工审稿、证据和敏感信息检查。
- 只试点少量外部提交；先衡量审稿成本和质量，再决定是否扩大。

完成门槛：

- 新贡献者可以仅按文档完成一个通过自动检查的候选 Skill。
- 至少完成一次端到端贡献试点，并记录审稿成本、退回原因和质量结论。
- 未建立稳定审核能力前，不开放无门槛投稿或自动发布。

## 明确暂缓

- 账号、收藏、评论、排行榜、付费、CMS 和服务端数据库。
- 站内直接运行 Agent 或代管用户工作材料。
- 在缺少搜索与反馈证据时批量增加 Skill。
- 在内容质量和安装闭环尚未验证前投入付费获客。
- 仅为了“更像平台”而增加社区、推荐算法或复杂后台。

## 当前执行顺序

1. [x] 确认并发布当前本地 Launch Candidate。
2. [x] 完成 CI、发布版本约定和上线运行手册。
3. [ ] 增加三类反馈 Issue 模板与站内入口。
4. [ ] 准备 3 个代表性 Skill 的案例节选和边界说明。
5. [ ] 备案通过后执行域名、HTTPS、监控、安装和收录验收。
