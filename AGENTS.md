# MARKET//SKILLS 项目规则

- 开始工作前阅读 `PROJECT.md` 与 `WORKLOG.md`，并以实际文件状态为准。
- 只做与当前目标直接相关的改动，优先选择最小、清晰、可验证的实现。
- 所有 Skill 必须符合 Open Agent Skills 目录结构，并通过 `npm run validate:skills`。
- 完成真实项目工作后更新 `PROJECT.md`，并把新日志插入 `WORKLOG.md` 的唯一追加锚点上方。
- 不在仓库中记录账号、密钥、令牌或未公开的工作材料。

